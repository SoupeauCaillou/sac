#pragma once

#include <iostream>
#define LOG(arg) (std::cout)
#define VLOG(arg) (std::cout)
#define VLOG_IF(arg1, ...) (std::cout)
#define LOG_IF(arg1, ...) (std::cout)
#define LOG_EVERY_N(arg1, ...) (std::cout)